<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sugorokuLibrary="clr-namespace:SugorokuLibrary;assembly=SugorokuLibrary"
             x:Class="SugorokuClientApp.WaitOtherPlayerPage" NavigationPage.HasNavigationBar="false">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="8*" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>
            <ListView x:Name="PlayersView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding PlayerName}"
                                  Detail="{Binding PlayerID}" x:DataType="sugorokuLibrary:Player" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Grid.Row="1" Text="ゲーム開始" x:Name="GameStartButton" Clicked="GameStartButtonClicked" />
        </Grid>
    </ContentPage.Content>
</ContentPage>